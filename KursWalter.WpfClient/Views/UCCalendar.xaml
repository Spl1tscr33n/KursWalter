<UserControl x:Class="KursWalter.WpfClient.UserControls.UCCalendar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="180">
    <Border Margin="1" MinWidth="185" BorderBrush="Black" BorderThickness="1">
        <Grid Margin="0 1">
            <Grid.RowDefinitions>
               <RowDefinition Height="Auto"/>
               <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button x:Name="btMinCalendar" Content="5" FontFamily="Marlett" VerticalAlignment="Top" Command="{Binding ExpandCommand}" Click="btMinCalendar_Click"/>
            <Calendar Grid.Row="1" Width="Auto" Height="Auto" Margin="0 -2" Visibility="{Binding IsContentVisible}" />

     </Grid>
    </Border>
</UserControl>
