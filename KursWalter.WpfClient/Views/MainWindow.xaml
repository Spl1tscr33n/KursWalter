<Window x:Class="KursWalter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Man nennt mich KursWalter weil ich die Kurse verwalte alter" Height="700" Width="950" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl ContentTemplate="{DynamicResource HeaderLogoViewModelTemplate}"/>
        <ContentControl Grid.Column="1" HorizontalAlignment="Right" ContentTemplate="{DynamicResource SearchViewModelTemplate}"/>
        <ListBox Grid.Column="0" Grid.Row="1">
            <ContentControl Width="Auto" ContentTemplate="{StaticResource LoginViewModelTemplate}"/>
            <ContentControl Width="Auto" ContentTemplate="{StaticResource CalendarViewModelTemplate}"/>
        </ListBox>
                <ListBox Grid.Column="1" Grid.Row="1">
                    <ContentControl ContentTemplate="{DynamicResource InfoFrameViewModelTemplate}"/>
                </ListBox>
    </Grid>
</Window>
